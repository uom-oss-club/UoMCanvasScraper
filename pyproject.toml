[project]
name = "UoM Canvas Scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "inquirer3>=0.6.1",
    "pytest>=8.4.2",
]
[tool.pytest.ini_option]
pythonpath = [
    "src"
]
